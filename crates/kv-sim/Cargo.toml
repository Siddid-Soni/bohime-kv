# Deterministic simulator: virtual clock, seeded RNG, lossy in-memory network,
# fault injection. Drives kv-raft's pure Action/Ready interface (see plan
# Part 2) instead of real I/O.
[package]
name = "kv-sim"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
kv-raft.workspace = true
rand.workspace = true
tracing.workspace = true

[dev-dependencies]
proptest.workspace = true
