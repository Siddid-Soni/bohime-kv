//! `kv-client`: smart client library + CLI (shard map cache, leader hints,
//! retry with backoff). Wiring begins at M6/M13.

fn main() {
    println!("bohime kv-client placeholder — implementation begins at M6");
}

#[cfg(test)]
mod tests;
